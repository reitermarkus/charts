{{/* Make sure all variables are set properly */}}
{{- include "common.values.setup" . }}

{{/* Append the hardcoded settings */}}
{{- define "zero-scale-proxy.hardcodedValues" -}}
{{- .Values.global | toYaml }}
{{- end -}}
{{- $_ := mergeOverwrite .Values (include "zero-scale-proxy.hardcodedValues" . | fromYaml) -}}

{{/* Render the templates */}}
{{ include "common.all" . }}
